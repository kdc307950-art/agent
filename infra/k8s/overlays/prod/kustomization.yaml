apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - ingress.yaml
  - network-policy.yaml
  - pdb.yaml
patches:
  - path: deployment-patch.yaml
  - path: migration-job.yaml
images:
  # 生产固定 digest：先推镜像并记录 digest，再替换下面的引用（示例值不可用）。
  - name: ghcr.io/your-org/langgraph-agent
    digest: sha256:REPLACE_WITH_REAL_DIGEST
